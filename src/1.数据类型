Java的整数类型：
一.int 整数
二.double 小数
(1)double的类型操作和int相似
(2)double的计算结果一般都是double类型
(3)'='是赋值操作，把右边的结果赋值给左边的变量
三.boolean 布尔值
(1)保存一种状态，不成立(false),成立(true)
(2)取值范围：true->真；false->假
(3)主要用来做条件判断
四.String 字符串
(1)在Java中使用双引号""来描述字符串
(2)String字符串名称首字母大写(相较于其他数据类型)
(3)字符串可以执行+操作,表示字符串的拼接